1.元组可以存储多个数据元素，且数据不可修改

2.定义元组可以添加多个元素，也可以添加单个元素（必须加逗号），否则会改变元组类型为单个数据元素的类型

3.查找
3.1 通过下标查找

3.2 index():查找某个元素，存在返回下标
tuple.index('xxx')

3.3 count():统计某个元素出现次数
tuple.index('xxx')

3.4 len():统计元组长度
tuple.len()




